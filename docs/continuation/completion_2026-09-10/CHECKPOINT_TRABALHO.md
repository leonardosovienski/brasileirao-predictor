> Checkpoint intermediário preservado. Para o estado posterior desta rodada, consultar REGISTROS.json e RESULTADO.md.

# CPL-20260910 — checkpoint de trabalho (revisão em andamento)

Base preservada: main/456b9025cfa3a596e754088ec3001fbeb5fcc7de. Este documento não declara conclusão integral. Artefatos desta rodada: C:/BRASILEIRAO/work/completion-2026-09-10. Nenhum agente, aposta, ordem, aquisição autenticada ou acesso a dados operacionais/protegidos. Nenhum commit desta rodada ainda.

## Correções implementadas e evidência até este checkpoint

| ID | Alegação afetada / problema demonstrado | Correção e teste | Estado |
|---|---|---|---|
| CPL01 | `predict.show` calculava duas vezes e a exibição perdia a data usada no log | `display.from_prediction` recebe o mesmo resultado e odds já registrados; meta conserva data. `regressions-before` falha por duas chamadas; `regressions-after` passa | validado no ensaio sintético |
| CPL02 | Dois payloads bitemporais com mesmos clocks eram arbitrados pelo hash | Empate material agora levanta conflito, sem escolher um resultado pelo valor do hash | validado |
| CPL03 | Primeira página Sportmonks era tratada como toda a cobertura; liga ausente nela era declarada fora do plano | Paginação com orçamento explícito (padrão continua1 consulta); suporta cursor atual e paginação legada; orçamento insuficiente informa incompletude. Documentação primária atual diverge do resultado indexado antigo | validado parcialmente; ampliar testes de cursor e orçamento |
| CPL04 | Configurar faixa de edge bastava para a interface afirmar confiança ALTA e faixa historicamente validada | Estado NÃO VALIDADA; odds agregadas identificadas como diagnóstico; nenhum capital autorizado | validado |
| CPL05 | Escalação publicada antes mas recebida depois do corte entrava na feature; recibo ausente era ignorado | `lineup_state_asof` exige ingestão <= corte, separa publicação desconhecida, recusa empate de vintages | validado no lote; completar revisão de persistência/estado vazio |
| CPL06 | API-Football inventava published_at = relógio local antes da requisição | Esquema lineup/2: publicação null, observação/ingestão ao terminar; relógio injetado permanece explícito para ensaio | validado |
| CPL07 | Sportmonks descartava starting_at sem sufixo, embora o contrato defina UTC | Requisição fixa timezone=UTC; conversão sob esse contrato | validado sintético; sem aquisição autenticada |
| CPL08 | Consenso podia combinar partidas/linhas diferentes e chamava preços de executáveis | Rejeita identidades/linhas misturadas; preço rotulado somente observado; economia false | validado; falta excluir ofertante explicitamente quando usado como referência de EV |
| CPL09 | Adaptador The Odds API/1 rotula odds recebidas após kickoff como PROSPECTIVE_ELIGIBLE | Adaptador compartilhado H9 preservado. Novo `data/odds_api_snapshot.py` independente e offline conserva recibo completo/vazio/inválido, IDs, clocks e mercados. Sem autenticação, banco ou seleção de capital | 14 testes; falta integração CLI e documentação |
| CPL10 | NB do total de eventos não correspondia à soma das duas contagens ajustadas | Convolução das PMFs/CDFs individuais; exemplo P(total=0) antigo0,154625 versus correto0,128184 | validado; sem reavaliar performance histórica |
| CPL11 | MarketPricer aceitava massa negativa, NaN, grade vazia ou soma4; AH aceitava linhas inválidas | Contratos de medida de probabilidade e quartos de gol, sem normalizar silenciosamente entrada inválida | validado |
| CPL12 | Prontidão live aceitava placar negativo | Rejeição de contagens negativas e clubes iguais | validado; demais evidências continuam declarações do chamador |
| CPL13 | CLI `prever` recalculava previsão e prosseguia mesmo se log falhasse | Fluxo cheio reutiliza `predict.show`, com uma previsão/log, e falha se não persistir | validado; fluxo de período/configuração/cache ainda em revisão |
| CPL14 | Livro manual aceitava NaN/Inf/stake bool/seleção desconhecida/prob>1, ID duplicado, HT por time impossível; list/banca ligavam liquidação pela posição | Validadores antes de append; índice por bet_id e fallback apenas legado; capital disponível separado de exposição. Contabilidade rotulada bruta em unidades atuais, custos não reconciliados | corrigido; lote ledger-after em validação neste checkpoint |

Lotes concluídos: regressions-after22 passou; source-regressions-after30 passou; odds-receipt-tests14 passou; math-consumer-after50 passou. Há repetição entre lotes: não somar como testes únicos. Baselines de falha preservados em XML e isolation.json. `confidence-before` reproduziu ALTA depois de corrigir a assinatura do próprio teste; primeira execução desse teste tinha erro do ensaio (KeyError), conservado. Fonte-regressions-before5 falhas; math-consumer-before11; ledger-before9. Toda execução em SQLite sintético/temp isolado, rede e subprocessos vedados pelo runner.

## Limites e próximos trabalhos necessários

- Inventário atual inicial437 arquivos;57 com contrato/metadados protegidos (operational_readiness acrescentado). Inventário não é revisão semântica. `inventory.json` guarda hashes e imports; import contracts de arquivos protegidos foram extraídos sem executar corpos ou ler observações/resultados. Descoberto H9FrozenPolicyEvaluator dentro de serving_evaluator: preservar módulo e execução; não executar classe nem ler resultados. Classificação por nome de arquivo era insuficiente.
- `db`, `ratings`, `cron_update_models`, `model`, `xg_model`, identidade e coletores compartilhados preservados. DB legado reescreve placares e mantém primeiro result_observed_at; cache usa contagem/config, não hash dos dados, e grava Elo/params separadamente. Isso não prova replay de revisões nem atomicidade da versão do modelo. Resolver consumo independente seguro ou bloquear o uso econômico, sem mexer na coleta protegida.
- `_market_probs` ainda é associação aproximada por nomes/±3 dias e sem disponibilidade. Mantido para consumidores legados/diagnóstico; não serve como oferta executável. `market_anchor.persist_market_observations`, importado pelo coletor H9, não foi alterado; conferir hash AST da função no fechamento.
- CSV BE e177 históricos DC já íntegros não foram baixados de novo. Nenhum novo lucro foi medido. BE é contexto congelado:226 envelopes anônimos positivos condicionalmente, modelo de gols sem suporte; falta observação nominal/simultânea/exequível. Captura DC11/09 23UTC e helpers/agenda/reserva20 permanecem intocados.
- Cinco GETs públicos documentais: The Odds API v4/200, Sportmonks paginação/200, primeira requisição/200, endereço errado de timezone retornou200 com Page Not Found (não contar como evidência de timezone), API-Football documentation-v3 HTTPError. Web abriu caminho correto `set-your-time-zone.md` e confirmou parâmetro `timezone`, UTC padrão. Preservar recibo/raw adicional correto e status HTTP da falha API-Football. Não repetir lote intacto.
- Terminar revisão por subsistema, registro central sucessor de RI/IE/BE, provas de fronteira, checks de lint/tipos/build/CLI, fontes/lacunas, verificação de automação existente sem alteração, guias e entrega/backup. Não confundir as correções acima com projeto pronto ou mandato inteiro concluído.
